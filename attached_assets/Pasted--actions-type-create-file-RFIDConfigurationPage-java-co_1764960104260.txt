{"actions": [
  {"type": "create", "file": "RFIDConfigurationPage.java", "content": "import javax.swing.*;\nimport javax.swing.border.*;\nimport java.awt.*;\nimport java.awt.event.*;\nimport java.io.*;\nimport java.util.Properties;\nimport javax.swing.table.DefaultTableModel;\n\n/**\n * Página de configuración GUI para el lector RFID Zebra FX7500\n * Permite configurar parámetros del lector, antenas y API REST\n */\npublic class RFIDConfigurationPage extends JFrame {\n    \n    // Archivo de configuración\n    private static final String CONFIG_FILE = \"rfid_config.properties\";\n    \n    // Properties para almacenar configuración\n    private Properties config;\n    \n    // Componentes de UI - Configuración del Lector\n    private JTextField txtReaderIP;\n    private JTextField txtReaderPort;\n    private JComboBox<String> cmbReaderMode;\n    private JComboBox<Integer> cmbSession;\n    private JComboBox<String> cmbSearchMode;\n    \n    // Componentes de UI - Configuración de Antenas\n    private JCheckBox[] chkAntenas;\n    private JSpinner[] spnPotenciaTx;\n    private JSpinner[] spnSensibilidadRx;\n    private DefaultTableModel modeloTablaAntenas;\n    \n    // Componentes de UI - Configuración de API\n    private JTextField txtAPIEndpoint;\n    private JTextField txtAPIKey;\n    private JSpinner spnTimeout;\n    private JSpinner spnRetryAttempts;\n    private JCheckBox chkEnableSSL;\n    \n    // Componentes de UI - Filtros y Opciones Avanzadas\n    private JCheckBox chkIncludeRSSI;\n    private JCheckBox chkIncludePhase;\n    private JCheckBox chkIncludeTimestamp;\n    private JSpinner spnRSSIThreshold;\n    private JTextField txtTagFilter;\n    \n    // Botones de acción\n    private JButton btnGuardar;\n    private JButton btnCargar;\n    private JButton btnRestablecer;\n    private JButton btnProbarConexion;\n    private JButton btnProbarAPI;\n    \n    public RFIDConfigurationPage() {\n        config = new Properties();\n        initializeUI();\n        cargarConfiguracion();\n    }\n    \n    /**\n     * Inicializa la interfaz de usuario\n     */\n    private void initializeUI() {\n        setTitle(\"Configuración RFID - Zebra FX7500\");\n        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);\n        setSize(900, 700);\n        setLocationRelativeTo(null);\n        \n        // Panel principal con pestañas\n        JTabbedPane tabbedPane = new JTabbedPane();\n        \n        // Pestaña 1: Configuración del Lector\n        tabbedPane.addTab(\"Lector RFID\", createReaderPanel());\n        \n        // Pestaña 2: Configuración de Antenas\n        tabbedPane.addTab(\"Antenas\", createAntenasPanel());\n        \n        // Pestaña 3: Configuración de API\n        tabbedPane.addTab(\"API REST\", createAPIPanel());\n        \n        // Pestaña 4: Filtros y Opciones Avanzadas\n        tabbedPane.addTab(\"Opciones Avanzadas\", createAdvancedPanel());\n        \n        // Panel de botones en la parte inferior\n        JPanel panelBotones = createButtonPanel();\n        \n        // Panel de estado\n        JPanel panelEstado = createStatusPanel();\n        \n        // Layout principal\n        setLayout(new BorderLayout(10, 10));\n        add(tabbedPane, BorderLayout.CENTER);\n        add(panelBotones, BorderLayout.SOUTH);\n        add(panelEstado, BorderLayout.NORTH);\n        \n        // Añadir padding\n        ((JComponent) getContentPane()).setBorder(\n            BorderFactory.createEmptyBorder(10, 10, 10, 10));\n    }\n    \n    /**\n     * Crea el panel de configuración del lector\n     */\n    private JPanel createReaderPanel() {\n        JPanel panel = new JPanel(new GridBagLayout());\n        GridBagConstraints gbc = new GridBagConstraints();\n        gbc.insets = new Insets(5, 5, 5, 5);\n        gbc.fill = GridBagConstraints.HORIZONTAL;\n        \n        // Título\n        JLabel lblTitulo = new JLabel(\"Configuración del Lector RFID\");\n        lblTitulo.setFont(new Font(\"Arial\", Font.BOLD, 16));\n        gbc.gridx = 0; gbc.gridy = 0; gbc.gridwidth = 2;\n        panel.add(lblTitulo, gbc);\n        \n        gbc.gridwidth = 1;\n        \n        // IP del Lector\n        gbc.gridx = 0; gbc.gridy = 1;\n        panel.add(new JLabel(\"IP del Lector:\"), gbc);\n        gbc.gridx = 1;\n        txtReaderIP = new JTextField(\"192.168.1.100\", 20);\n        panel.add(txtReaderIP, gbc);\n        \n        // Puerto\n        gbc.gridx = 0; gbc.gridy = 2;\n        panel.add(new JLabel(\"Puerto:\"), gbc);\n        gbc.gridx = 1;\n        txtReaderPort = new JTextField(\"5084\", 20);\n        panel.add(txtReaderPort, gbc);\n        \n        // Modo del Lector\n        gbc.gridx = 0; gbc.gridy = 3;\n        panel.add(new JLabel(\"Modo del Lector:\"), gbc);\n        gbc.gridx = 1;\n        String[] modos = {\"AutoSetDenseReader\", \"MaxThroughput\", \"Hybrid\", \"DenseReaderM4\", \"DenseReaderM8\"};\n        cmbReaderMode = new JComboBox<>(modos);\n        panel.add(cmbReaderMode, gbc);\n        \n        // Sesión\n        gbc.gridx = 0; gbc.gridy = 4;\n        panel.add(new JLabel(\"Sesión:\"), gbc);\n        gbc.gridx = 1;\n        Integer[] sesiones = {0, 1, 2, 3};\n        cmbSession = new JComboBox<>(sesiones);\n        cmbSession.setSelectedItem(2);\n        panel.add(cmbSession, gbc);\n        \n        // Modo de Búsqueda\n        gbc.gridx = 0; gbc.gridy = 5;\n        panel.add(new JLabel(\"Modo de Búsqueda:\"), gbc);\n        gbc.gridx = 1;\n        String[] searchModes = {\"DualTarget\", \"SingleTarget\", \"SingleTargetReset\"};\n        cmbSearchMode = new JComboBox<>(searchModes);\n        panel.add(cmbSearchMode, gbc);\n        \n        // Espacio en blanco para empujar contenido hacia arriba\n        gbc.gridx = 0; gbc.gridy = 6; gbc.gridwidth = 2;\n        gbc.weighty = 1.0;\n        panel.add(new JLabel(), gbc);\n        \n        panel.setBorder(BorderFactory.createEmptyBorder(20, 20, 20, 20));\n        return panel;\n    }\n    \n    /**\n     * Crea el panel de configuración de antenas\n     */\n    private JPanel createAntenasPanel() {\n        JPanel panel = new JPanel(new BorderLayout(10, 10));\n        \n        // Título\n        JLabel lblTitulo = new JLabel(\"Configuración de Antenas (1-4)\");\n        lblTitulo.setFont(new Font(\"Arial\", Font.BOLD, 16));\n        lblTitulo.setBorder(BorderFactory.createEmptyBorder(10, 0, 10, 0));\n        panel.add(lblTitulo, BorderLayout.NORTH);\n        \n        // Tabla de antenas\n        String[] columnas = {\"Antena\", \"Habilitada\", \"Potencia TX (dBm)\", \"Sensibilidad RX (dBm)\"};\n        modeloTablaAntenas = new DefaultTableModel(columnas, 0) {\n            @Override\n            public Class<?> getColumnClass(int column) {\n                if (column == 1) return Boolean.class;\n                return String.class;\n            }\n        };\n        \n        JTable tablaAntenas = new JTable(modeloTablaAntenas);\n        tablaAntenas.setRowHeight(30);\n        \n        // Inicializar arrays\n        chkAntenas = new JCheckBox[4];\n        spnPotenciaTx = new JSpinner[4];\n        spnSensibilidadRx = new JSpinner[4];\n        \n        // Añadir filas para cada antena\n        for (int i = 0; i < 4; i++) {\n            modeloTablaAntenas.addRow(new Object[]{\"Antena \" + (i + 1), true, \"30.0\", \"-70\"});\n        }\n        \n        JScrollPane scrollPane = new JScrollPane(tablaAntenas);\n        panel.add(scrollPane, BorderLayout.CENTER);\n        \n        // Panel de información\n        JPanel infoPanel = new JPanel();\n        infoPanel.setLayout(new BoxLayout(infoPanel, BoxLayout.Y_AXIS));\n        infoPanel.setBorder(BorderFactory.createTitledBorder(\"Información\"));\n        infoPanel.add(new JLabel(\"• Potencia TX: Rango típico 10-30 dBm\"));\n        infoPanel.add(new JLabel(\"• Sensibilidad RX: Rango típico -80 a -60 dBm\"));\n        infoPanel.add(new JLabel(\"• Mayor potencia = Mayor alcance (más interferencia)\"));\n        infoPanel.add(new JLabel(\"• Mayor sensibilidad = Mejor detección de tags débiles\"));\n        panel.add(infoPanel, BorderLayout.SOUTH);\n        \n        panel.setBorder(BorderFactory.createEmptyBorder(20, 20, 20, 20));\n        return panel;\n    }\n    \n    /**\n     * Crea el panel de configuración de API\n     */\n    private JPanel createAPIPanel() {\n        JPanel panel = new JPanel(new GridBagLayout());\n        GridBagConstraints gbc = new GridBagConstraints();\n        gbc.insets = new Insets(5, 5, 5, 5);\n        gbc.fill = GridBagConstraints.HORIZONTAL;\n        \n        // Título\n        JLabel lblTitulo = new JLabel(\"Configuración de API REST\");\n        lblTitulo.setFont(new Font(\"Arial\", Font.BOLD, 16));\n        gbc.gridx = 0; gbc.gridy = 0; gbc.gridwidth = 2;\n        panel.add(lblTitulo, gbc);\n        \n        gbc.gridwidth = 1;\n        \n        // Endpoint de la API\n        gbc.gridx = 0; gbc.gridy = 1;\n        panel.add(new JLabel(\"Endpoint URL:\"), gbc);\n        gbc.gridx = 1;\n        txtAPIEndpoint = new JTextField(\"http://localhost:8080/api/rfid/tags\", 30);\n        panel.add(txtAPIEndpoint, gbc);\n        \n        // API Key\n        gbc.gridx = 0; gbc.gridy = 2;\n        panel.add(new JLabel(\"API Key:\"), gbc);\n        gbc.gridx = 1;\n        txtAPIKey = new JTextField(20);\n        panel.add(txtAPIKey, gbc);\n        \n        // SSL/TLS\n        gbc.gridx = 0; gbc.gridy = 3; gbc.gridwidth = 2;\n        chkEnableSSL = new JCheckBox(\"Habilitar SSL/TLS (HTTPS)\");\n        panel.add(chkEnableSSL, gbc);\n        \n        gbc.gridwidth = 1;\n        \n        // Timeout\n        gbc.gridx = 0; gbc.gridy = 4;\n        panel.add(new JLabel(\"Timeout (segundos):\"), gbc);\n        gbc.gridx = 1;\n        spnTimeout = new JSpinner(new SpinnerNumberModel(10, 1, 60, 1));\n        panel.add(spnTimeout, gbc);\n        \n        // Reintentos\n        gbc.gridx = 0; gbc.gridy = 5;\n        panel.add(new JLabel(\"Reintentos:\"), gbc);\n        gbc.gridx = 1;\n        spnRetryAttempts = new JSpinner(new SpinnerNumberModel(3, 0, 10, 1));\n        panel.add(spnRetryAttempts, gbc);\n        \n        // Botón de prueba de API\n        gbc.gridx = 0; gbc.gridy = 6; gbc.gridwidth = 2;\n        btnProbarAPI = new JButton(\"Probar Conexión a API\");\n        btnProbarAPI.addActionListener(e -> probarConexionAPI());\n        panel.add(btnProbarAPI, gbc);\n        \n        // Espacio\n        gbc.gridy = 7; gbc.weighty = 1.0;\n        panel.add(new JLabel(), gbc);\n        \n        panel.setBorder(BorderFactory.createEmptyBorder(20, 20, 20, 20));\n        return panel;\n    }\n    \n    /**\n     * Crea el panel de opciones avanzadas\n     */\n    private JPanel createAdvancedPanel() {\n        JPanel panel = new JPanel(new GridBagLayout());\n        GridBagConstraints gbc = new GridBagConstraints();\n        gbc.insets = new Insets(5, 5, 5, 5);\n        gbc.fill = GridBagConstraints.HORIZONTAL;\n        gbc.anchor = GridBagConstraints.WEST;\n        \n        // Título\n        JLabel lblTitulo = new JLabel(\"Filtros y Opciones Avanzadas\");\n        lblTitulo.setFont(new Font(\"Arial\", Font.BOLD, 16));\n        gbc.gridx = 0; gbc.gridy = 0; gbc.gridwidth = 2;\n        panel.add(lblTitulo, gbc);\n        \n        // Sección: Datos a Incluir\n        gbc.gridy = 1;\n        JLabel lblIncluir = new JLabel(\"Datos a incluir en el reporte:\");\n        lblIncluir.setFont(new Font(\"Arial\", Font.BOLD, 12));\n        panel.add(lblIncluir, gbc);\n        \n        gbc.gridwidth = 1;\n        gbc.gridx = 0; gbc.gridy = 2;\n        chkIncludeRSSI = new JCheckBox(\"RSSI (Intensidad de señal)\", true);\n        panel.add(chkIncludeRSSI, gbc);\n        \n        gbc.gridy = 3;\n        chkIncludePhase = new JCheckBox(\"Ángulo de fase\", true);\n        panel.add(chkIncludePhase, gbc);\n        \n        gbc.gridy = 4;\n        chkIncludeTimestamp = new JCheckBox(\"Timestamps (primera/última lectura)\", true);\n        panel.add(chkIncludeTimestamp, gbc);\n        \n        // Umbral de RSSI\n        gbc.gridx = 0; gbc.gridy = 5;\n        panel.add(new JLabel(\"Umbral mínimo de RSSI (dBm):\"), gbc);\n        gbc.gridx = 1;\n        spnRSSIThreshold = new JSpinner(new SpinnerNumberModel(-80, -100, 0, 1));\n        panel.add(spnRSSIThreshold, gbc);\n        \n        // Filtro de tags\n        gbc.gridx = 0; gbc.gridy = 6;\n        panel.add(new JLabel(\"Filtro de EPC (regex):\"), gbc);\n        gbc.gridx = 1;\n        txtTagFilter = new JTextField(20);\n        txtTagFilter.setToolTipText(\"Ejemplo: ^E280.*$ para tags que empiecen con E280\");\n        panel.add(txtTagFilter, gbc);\n        \n        // Información adicional\n        gbc.gridx = 0; gbc.gridy = 7; gbc.gridwidth = 2;\n        JTextArea txtInfo = new JTextArea(\n            \"Nota: El filtro de EPC usa expresiones regulares (regex).\\n\" +\n            \"Ejemplos:\\n\" +\n            \"  - ^E280.*$ : Tags que empiezan con E280\\n\" +\n            \"  - .*1234$ : Tags que terminan con 1234\\n\" +\n            \"  - .* : Todos los tags (sin filtro)\"\n        );\n        txtInfo.setEditable(false);\n        txtInfo.setBackground(panel.getBackground());\n        txtInfo.setBorder(BorderFactory.createTitledBorder(\"Ayuda\"));\n        panel.add(txtInfo, gbc);\n        \n        // Espacio\n        gbc.gridy = 8; gbc.weighty = 1.0;\n        panel.add(new JLabel(), gbc);\n        \n        panel.setBorder(BorderFactory.createEmptyBorder(20, 20, 20, 20));\n        return panel;\n    }\n    \n    /**\n     * Crea el panel de botones de acción\n     */\n    private JPanel createButtonPanel() {\n        JPanel panel = new JPanel(new FlowLayout(FlowLayout.RIGHT, 10, 10));\n        \n        btnProbarConexion = new JButton(\"Probar Conexión Lector\");\n        btnProbarConexion.addActionListener(e -> probarConexionLector());\n        \n        btnRestablecer = new JButton(\"Restablecer Valores\");\n        btnRestablecer.addActionListener(e -> restablecerValoresPorDefecto());\n        \n        btnCargar = new JButton(\"Cargar Configuración\");\n        btnCargar.addActionListener(e -> cargarConfiguracion());\n        \n        btnGuardar = new JButton(\"Guardar Configuración\");\n        btnGuardar.addActionListener(e -> guardarConfiguracion());\n        btnGuardar.setBackground(new Color(76, 175, 80));\n        btnGuardar.setForeground(Color.WHITE);\n        btnGuardar.setFocusPainted(false);\n        \n        panel.add(btnProbarConexion);\n        panel.add(btnRestablecer);\n        panel.add(btnCargar);\n        panel.add(btnGuardar);\n        \n        return panel;\n    }\n    \n    /**\n     * Crea el panel de estado\n     */\n    private JPanel createStatusPanel() {\n        JPanel panel = new JPanel(new BorderLayout());\n        panel.setBorder(BorderFactory.createCompoundBorder(\n            BorderFactory.createLineBorder(Color.LIGHT_GRAY),\n            BorderFactory.createEmptyBorder(5, 10, 5, 10)\n        ));\n        \n        JLabel lblStatus = new JLabel(\"⚙ Sistema listo para configurar\");\n        lblStatus.setFont(new Font(\"Arial\", Font.PLAIN, 12));\n        panel.add(lblStatus, BorderLayout.WEST);\n        \n        return panel;\n    }\n    \n    /**\n     * Guarda la configuración actual en archivo\n     */\n    private void guardarConfiguracion() {\n        try {\n            // Configuración del lector\n            config.setProperty(\"reader.ip\", txtReaderIP.getText());\n            config.setProperty(\"reader.port\", txtReaderPort.getText());\n            config.setProperty(\"reader.mode\", (String) cmbReaderMode.getSelectedItem());\n            config.setProperty(\"reader.session\", cmbSession.getSelectedItem().toString());\n            config.setProperty(\"reader.searchMode\", (String) cmbSearchMode.getSelectedItem());\n            \n            // Configuración de antenas\n            for (int i = 0; i < 4; i++) {\n                config.setProperty(\"antenna.\" + (i+1) + \".enabled\", \n                    modeloTablaAntenas.getValueAt(i, 1).toString());\n                config.setProperty(\"antenna.\" + (i+1) + \".txPower\", \n                    modeloTablaAntenas.getValueAt(i, 2).toString());\n                config.setProperty(\"antenna.\" + (i+1) + \".rxSensitivity\", \n                    modeloTablaAntenas.getValueAt(i, 3).toString());\n            }\n            \n            // Configuración de API\n            config.setProperty(\"api.endpoint\", txtAPIEndpoint.getText());\n            config.setProperty(\"api.key\", txtAPIKey.getText());\n            config.setProperty(\"api.enableSSL\", String.valueOf(chkEnableSSL.isSelected()));\n            config.setProperty(\"api.timeout\", spnTimeout.getValue().toString());\n            config.setProperty(\"api.retryAttempts\", spnRetryAttempts.getValue().toString());\n            \n            // Opciones avanzadas\n            config.setProperty(\"report.includeRSSI\", String.valueOf(chkIncludeRSSI.isSelected()));\n            config.setProperty(\"report.includePhase\", String.valueOf(chkIncludePhase.isSelected()));\n            config.setProperty(\"report.includeTimestamp\", String.valueOf(chkIncludeTimestamp.isSelected()));\n            config.setProperty(\"filter.rssiThreshold\", spnRSSIThreshold.getValue().toString());\n            config.setProperty(\"filter.epcRegex\", txtTagFilter.getText());\n            \n            // Guardar en archivo\n            try (FileOutputStream fos = new FileOutputStream(CONFIG_FILE)) {\n                config.store(fos, \"Configuración RFID - Zebra FX7500\");\n            }\n            \n            JOptionPane.showMessageDialog(this,\n                \"Configuración guardada exitosamente en: \" + CONFIG_FILE,\n                \"Éxito\",\n                JOptionPane.INFORMATION_MESSAGE);\n                \n        } catch (Exception e) {\n            JOptionPane.showMessageDialog(this,\n                \"Error al guardar configuración: \" + e.getMessage(),\n                \"Error\",\n                JOptionPane.ERROR_MESSAGE);\n        }\n    }\n    \n    /**\n     * Carga la configuración desde archivo\n     */\n    private void cargarConfiguracion() {\n        File configFile = new File(CONFIG_FILE);\n        if (!configFile.exists()) {\n            restablecerValoresPorDefecto();\n            return;\n        }\n        \n        try (FileInputStream fis = new FileInputStream(configFile)) {\n            config.load(fis);\n            \n            // Cargar configuración del lector\n            txtReaderIP.setText(config.getProperty(\"reader.ip\", \"192.168.1.100\"));\n            txtReaderPort.setText(config.getProperty(\"reader.port\", \"5084\"));\n            cmbReaderMode.setSelectedItem(config.getProperty(\"reader.mode\", \"AutoSetDenseReader\"));\n            cmbSession.setSelectedItem(Integer.parseInt(config.getProperty(\"reader.session\", \"2\")));\n            cmbSearchMode.setSelectedItem(config.getProperty(\"reader.searchMode\", \"DualTarget\"));\n            \n            // Cargar configuración de antenas\n            for (int i = 0; i < 4; i++) {\n                modeloTablaAntenas.setValueAt(\n                    Boolean.parseBoolean(config.getProperty(\"antenna.\" + (i+1) + \".enabled\", \"true\")),\n                    i, 1);\n                modeloTablaAntenas.setValueAt(\n                    config.getProperty(\"antenna.\" + (i+1) + \".txPower\", \"30.0\"),\n                    i, 2);\n                modeloTablaAntenas.setValueAt(\n                    config.getProperty(\"antenna.\" + (i+1) + \".rxSensitivity\", \"-70\"),\n                    i, 3);\n            }\n            \n            // Cargar configuración de API\n            txtAPIEndpoint.setText(config.getProperty(\"api.endpoint\", \"http://localhost:8080/api/rfid/tags\"));\n            txtAPIKey.setText(config.getProperty(\"api.key\", \"\"));\n            chkEnableSSL.setSelected(Boolean.parseBoolean(config.getProperty(\"api.enableSSL\", \"false\")));\n            spnTimeout.setValue(Integer.parseInt(config.getProperty(\"api.timeout\", \"10\")));\n            spnRetryAttempts.setValue(Integer.parseInt(config.getProperty(\"api.retryAttempts\", \"3\")));\n            \n            // Cargar opciones avanzadas\n            chkIncludeRSSI.setSelected(Boolean.parseBoolean(config.getProperty(\"report.includeRSSI\", \"true\")));\n            chkIncludePhase.setSelected(Boolean.parseBoolean(config.getProperty(\"report.includePhase\", \"true\")));\n            chkIncludeTimestamp.setSelected(Boolean.parseBoolean(config.getProperty(\"report.includeTimestamp\", \"true\")));\n            spnRSSIThreshold.setValue(Integer.parseInt(config.getProperty(\"filter.rssiThreshold\", \"-80\")));\n            txtTagFilter.setText(config.getProperty(\"filter.epcRegex\", \"\"));\n            \n            JOptionPane.showMessageDialog(this,\n                \"Configuración cargada exitosamente\",\n                \"Éxito\",\n                JOptionPane.INFORMATION_MESSAGE);\n                \n        } catch (Exception e) {\n            JOptionPane.showMessageDialog(this,\n                \"Error al cargar configuración: \" + e.getMessage(),\n                \"Error\",\n                JOptionPane.ERROR_MESSAGE);\n        }\n    }\n    \n    /**\n     * Restablece los valores por defecto\n     */\n    private void restablecerValoresPorDefecto() {\n        int confirm = JOptionPane.showConfirmDialog(this,\n            \"¿Está seguro de restablecer todos los valores por defecto?\",\n            \"Confirmar\",\n            JOptionPane.YES_NO_OPTION);\n            \n        if (confirm == JOptionPane.YES_OPTION) {\n            txtReaderIP.setText(\"192.168.1.100\");\n            txtReaderPort.setText(\"5084\");\n            cmbReaderMode.setSelectedItem(\"AutoSetDenseReader\");\n            cmbSession.setSelectedItem(2);\n            cmbSearchMode.setSelectedItem(\"DualTarget\");\n            \n            for (int i = 0; i < 4; i++) {\n                modeloTablaAntenas.setValueAt(true, i, 1);\n                modeloTablaAntenas.setValueAt(\"30.0\", i, 2);\n                modeloTablaAntenas.setValueAt(\"-70\", i, 3);\n            }\n            \n            txtAPIEndpoint.setText(\"http://localhost:8080/api/rfid/tags\");\n            txtAPIKey.setText(\"\");\n            chkEnableSSL.setSelected(false);\n            spnTimeout.setValue(10);\n            spnRetryAttempts.setValue(3);\n            \n            chkIncludeRSSI.setSelected(true);\n            chkIncludePhase.setSelected(true);\n            chkIncludeTimestamp.setSelected(true);\n            spnRSSIThreshold.setValue(-80);\n            txtTagFilter.setText(\"\");\n        }\n    }\n    \n    /**\n     * Prueba la conexión con el lector RFID\n     */\n    private void probarConexionLector() {\n        // Aquí implementarías la lógica real de conexión\n        JOptionPane.showMessageDialog(this,\n            \"Probando conexión con: \" + txtReaderIP.getText() + \":\" + txtReaderPort.getText() +\n            \"\\n\\nNota: Implementar lógica de conexión real\",\n            \"Prueba de Conexión\",\n            JOptionPane.INFORMATION_MESSAGE);\n    }\n    \n    /**\n     * Prueba la conexión con la API\n     */\n    private void probarConexionAPI() {\n        // Aquí implementarías la lógica real de prueba de API\n        JOptionPane.showMessageDialog(this,\n            \"Probando conexión con API: \" + txtAPIEndpoint.getText() +\n            \"\\n\\nNota: Implementar lógica de prueba real\",\n            \"Prueba de API\",\n            JOptionPane.INFORMATION_MESSAGE);\n    }\n    \n    /**\n     * Obtiene la configuración actual como Properties\n     */\n    public Properties getConfiguration() {\n        return config;\n    }\n    \n    /**\n     * Método main para ejecutar la ventana de configuración\n     */\n    public static void main(String[] args) {\n        SwingUtilities.invokeLater(() -> {\n            try {\n                UIManager.setLookAndF