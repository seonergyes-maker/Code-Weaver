Voy a crear un panel de configuración completo con interfaz gráfica usando Swing. Te proporcionaré varios archivos:

ConfigPanel.java - Panel principal de configuración
ConfigurationData.java - Modelo de datos de configuración
ConfigManager.java - Gestor para guardar/cargar configuración
ZebraRFIDConnectorApp.java - Versión modificada que usa la configuración
{"actions": [
  {
    "type": "create",
    "file": "ConfigurationData.java",
    "content": "import java.io.Serializable;\nimport java.util.HashMap;\nimport java.util.Map;\n\n/**\n * Clase que almacena toda la configuración de la aplicación RFID\n */\npublic class ConfigurationData implements Serializable {\n    private static final long serialVersionUID = 1L;\n    \n    // Configuración del lector\n    private String readerHostname = \"192.168.1.100\";\n    private int readerPort = 5084;\n    \n    // Configuración de la API\n    private String apiEndpoint = \"http://localhost:8080/api/rfid/tags\";\n    private String apiKey = \"\";\n    private int apiTimeout = 10; // segundos\n    \n    // Configuración de antenas\n    private Map<Integer, AntennaConfig> antennaConfigs = new HashMap<>();\n    \n    // Configuración de filtros\n    private double rssiCutoff = -70.0; // dBm\n    private boolean useHexFormat = false; // true=Hexadecimal, false=Decimal\n    private int sessionNumber = 2;\n    private String searchMode = \"DualTarget\";\n    \n    // Configuración de reporte\n    private int reportInterval = 1000; // milisegundos\n    private boolean sendOnlyNewTags = false;\n    \n    public ConfigurationData() {\n        // Inicializar configuración por defecto para 4 antenas\n        for (int i = 1; i <= 4; i++) {\n            antennaConfigs.put(i, new AntennaConfig(i));\n        }\n    }\n    \n    // Getters y Setters\n    public String getReaderHostname() { return readerHostname; }\n    public void setReaderHostname(String readerHostname) { this.readerHostname = readerHostname; }\n    \n    public int getReaderPort() { return readerPort; }\n    public void setReaderPort(int readerPort) { this.readerPort = readerPort; }\n    \n    public String getApiEndpoint() { return apiEndpoint; }\n    public void setApiEndpoint(String apiEndpoint) { this.apiEndpoint = apiEndpoint; }\n    \n    public String getApiKey() { return apiKey; }\n    public void setApiKey(String apiKey) { this.apiKey = apiKey; }\n    \n    public int getApiTimeout() { return apiTimeout; }\n    public void setApiTimeout(int apiTimeout) { this.apiTimeout = apiTimeout; }\n    \n    public Map<Integer, AntennaConfig> getAntennaConfigs() { return antennaConfigs; }\n    public void setAntennaConfigs(Map<Integer, AntennaConfig> antennaConfigs) { \n        this.antennaConfigs = antennaConfigs; \n    }\n    \n    public AntennaConfig getAntennaConfig(int antennaId) {\n        return antennaConfigs.get(antennaId);\n    }\n    \n    public double getRssiCutoff() { return rssiCutoff; }\n    public void setRssiCutoff(double rssiCutoff) { this.rssiCutoff = rssiCutoff; }\n    \n    public boolean isUseHexFormat() { return useHexFormat; }\n    public void setUseHexFormat(boolean useHexFormat) { this.useHexFormat = useHexFormat; }\n    \n    public int getSessionNumber() { return sessionNumber; }\n    public void setSessionNumber(int sessionNumber) { this.sessionNumber = sessionNumber; }\n    \n    public String getSearchMode() { return searchMode; }\n    public void setSearchMode(String searchMode) { this.searchMode = searchMode; }\n    \n    public int getReportInterval() { return reportInterval; }\n    public void setReportInterval(int reportInterval) { this.reportInterval = reportInterval; }\n    \n    public boolean isSendOnlyNewTags() { return sendOnlyNewTags; }\n    public void setSendOnlyNewTags(boolean sendOnlyNewTags) { \n        this.sendOnlyNewTags = sendOnlyNewTags; \n    }\n    \n    /**\n     * Clase interna para configuración de antena individual\n     */\n    public static class AntennaConfig implements Serializable {\n        private static final long serialVersionUID = 1L;\n        \n        private int antennaId;\n        private boolean enabled = true;\n        private double txPower = 30.0; // dBm (rango típico: 10-32.5)\n        private double rxSensitivity = -70.0; // dBm (rango típico: -80 a -50)\n        private boolean maxTxPower = false;\n        private boolean maxRxSensitivity = false;\n        \n        public AntennaConfig(int antennaId) {\n            this.antennaId = antennaId;\n        }\n        \n        // Getters y Setters\n        public int getAntennaId() { return antennaId; }\n        public void setAntennaId(int antennaId) { this.antennaId = antennaId; }\n        \n        public boolean isEnabled() { return enabled; }\n        public void setEnabled(boolean enabled) { this.enabled = enabled; }\n        \n        public double getTxPower() { return txPower; }\n        public void setTxPower(double txPower) { \n            this.txPower = Math.max(10.0, Math.min(32.5, txPower));\n        }\n        \n        public double getRxSensitivity() { return rxSensitivity; }\n        public void setRxSensitivity(double rxSensitivity) { \n            this.rxSensitivity = Math.max(-80.0, Math.min(-50.0, rxSensitivity));\n        }\n        \n        public boolean isMaxTxPower() { return maxTxPower; }\n        public void setMaxTxPower(boolean maxTxPower) { this.maxTxPower = maxTxPower; }\n        \n        public boolean isMaxRxSensitivity() { return maxRxSensitivity; }\n        public void setMaxRxSensitivity(boolean maxRxSensitivity) { \n            this.maxRxSensitivity = maxRxSensitivity; \n        }\n        \n        @Override\n        public String toString() {\n            return String.format(\"Antena %d: %s | TX: %.1f dBm | RX: %.1f dBm\",\n                antennaId, enabled ? \"Habilitada\" : \"Deshabilitada\", txPower, rxSensitivity);\n        }\n    }\n    \n    @Override\n    public String toString() {\n        return \"ConfigurationData{\" +\n                \"readerHostname='\" + readerHostname + '\\'' +\n                \", apiEndpoint='\" + apiEndpoint + '\\'' +\n                \", rssiCutoff=\" + rssiCutoff +\n                \", antenas=\" + antennaConfigs.size() +\n                '}';\n    }\n}"
  },
  {
    "type": "create",
    "file": "ConfigManager.java",
    "content": "import com.google.gson.Gson;\nimport com.google.gson.GsonBuilder;\nimport java.io.*;\nimport java.nio.file.Files;\nimport java.nio.file.Paths;\n\n/**\n * Gestor para guardar y cargar la configuración en formato JSON\n */\npublic class ConfigManager {\n    private static final String CONFIG_FILE = \"rfid_config.json\";\n    private static final Gson gson = new GsonBuilder()\n            .setPrettyPrinting()\n            .create();\n    \n    /**\n     * Guarda la configuración en un archivo JSON\n     */\n    public static boolean saveConfiguration(ConfigurationData config) {\n        try {\n            String json = gson.toJson(config);\n            Files.write(Paths.get(CONFIG_FILE), json.getBytes());\n            System.out.println(\"✓ Configuración guardada en: \" + CONFIG_FILE);\n            return true;\n        } catch (IOException e) {\n            System.err.println(\"✗ Error guardando configuración: \" + e.getMessage());\n            e.printStackTrace();\n            return false;\n        }\n    }\n    \n    /**\n     * Carga la configuración desde el archivo JSON\n     */\n    public static ConfigurationData loadConfiguration() {\n        File configFile = new File(CONFIG_FILE);\n        \n        if (!configFile.exists()) {\n            System.out.println(\"No se encontró archivo de configuración, usando valores por defecto\");\n            ConfigurationData defaultConfig = new ConfigurationData();\n            saveConfiguration(defaultConfig); // Crear archivo por defecto\n            return defaultConfig;\n        }\n        \n        try {\n            String json = new String(Files.readAllBytes(Paths.get(CONFIG_FILE)));\n            ConfigurationData config = gson.fromJson(json, ConfigurationData.class);\n            System.out.println(\"✓ Configuración cargada desde: \" + CONFIG_FILE);\n            return config;\n        } catch (IOException e) {\n            System.err.println(\"✗ Error cargando configuración: \" + e.getMessage());\n            e.printStackTrace();\n            return new ConfigurationData();\n        }\n    }\n    \n    /**\n     * Exporta la configuración a un archivo específico\n     */\n    public static boolean exportConfiguration(ConfigurationData config, String fileName) {\n        try {\n            String json = gson.toJson(config);\n            Files.write(Paths.get(fileName), json.getBytes());\n            System.out.println(\"✓ Configuración exportada a: \" + fileName);\n            return true;\n        } catch (IOException e) {\n            System.err.println(\"✗ Error exportando configuración: \" + e.getMessage());\n            return false;\n        }\n    }\n    \n    /**\n     * Importa configuración desde un archivo específico\n     */\n    public static ConfigurationData importConfiguration(String fileName) {\n        try {\n            String json = new String(Files.readAllBytes(Paths.get(fileName)));\n            ConfigurationData config = gson.fromJson(json, ConfigurationData.class);\n            System.out.println(\"✓ Configuración importada desde: \" + fileName);\n            return config;\n        } catch (IOException e) {\n            System.err.println(\"✗ Error importando configuración: \" + e.getMessage());\n            return null;\n        }\n    }\n    \n    /**\n     * Valida que la configuración sea correcta\n     */\n    public static boolean validateConfiguration(ConfigurationData config) {\n        if (config == null) {\n            System.err.println(\"✗ Configuración nula\");\n            return false;\n        }\n        \n        // Validar hostname\n        if (config.getReaderHostname() == null || config.getReaderHostname().trim().isEmpty()) {\n            System.err.println(\"✗ Hostname del lector no puede estar vacío\");\n            return false;\n        }\n        \n        // Validar puerto\n        if (config.getReaderPort() < 1 || config.getReaderPort() > 65535) {\n            System.err.println(\"✗ Puerto inválido: \" + config.getReaderPort());\n            return false;\n        }\n        \n        // Validar API endpoint\n        if (config.getApiEndpoint() == null || config.getApiEndpoint().trim().isEmpty()) {\n            System.err.println(\"✗ API Endpoint no puede estar vacío\");\n            return false;\n        }\n        \n        // Validar que al menos una antena esté habilitada\n        boolean anyAntennaEnabled = config.getAntennaConfigs().values().stream()\n                .anyMatch(ConfigurationData.AntennaConfig::isEnabled);\n        \n        if (!anyAntennaEnabled) {\n            System.err.println(\"✗ Debe haber al menos una antena habilitada\");\n            return false;\n        }\n        \n        System.out.println(\"✓ Configuración validada correctamente\");\n        return true;\n    }\n}"
  },
  {
    "type": "create",
    "file": "ConfigPanel.java",
    "content": "import javax.swing.*;\nimport javax.swing.border.*;\nimport javax.swing.filechooser.FileNameExtensionFilter;\nimport java.awt.*;\nimport java.awt.event.*;\nimport java.util.HashMap;\nimport java.util.Map;\n\n/**\n * Panel de configuración gráfico para el lector RFID Zebra FX7500\n */\npublic class ConfigPanel extends JFrame {\n    \n    private ConfigurationData currentConfig;\n    \n    // Componentes de la UI\n    private JTextField txtReaderIP;\n    private JTextField txtReaderPort;\n    private JTextField txtApiEndpoint;\n    private JPasswordField txtApiKey;\n    private JTextField txtApiTimeout;\n    private JTextField txtRssiCutoff;\n    private JComboBox<String> cmbSearchMode;\n    private JSpinner spnSession;\n    private JCheckBox chkHexFormat;\n    private JCheckBox chkOnlyNewTags;\n    private JTextField txtReportInterval;\n    \n    // Paneles de antenas\n    private Map<Integer, AntennaPanel> antennaPanels = new HashMap<>();\n    \n    // Botones de acción\n    private JButton btnSave;\n    private JButton btnLoad;\n    private JButton btnExport;\n    private JButton btnImport;\n    private JButton btnTestConnection;\n    private JButton btnApply;\n    private JButton btnReset;\n    \n    // Callback para cuando se aplique la configuración\n    private ConfigAppliedListener configAppliedListener;\n    \n    public ConfigPanel() {\n        this(new ConfigurationData());\n    }\n    \n    public ConfigPanel(ConfigurationData config) {\n        this.currentConfig = config;\n        initializeUI();\n        loadConfigToUI();\n    }\n    \n    private void initializeUI() {\n        setTitle(\"Configuración RFID - Zebra FX7500\");\n        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);\n        setLayout(new BorderLayout(10, 10));\n        \n        // Panel principal con pestañas\n        JTabbedPane tabbedPane = new JTabbedPane();\n        \n        // Pestaña 1: Conexión\n        tabbedPane.addTab(\"Conexión\", createConnectionPanel());\n        \n        // Pestaña 2: API\n        tabbedPane.addTab(\"API\", createAPIPanel());\n        \n        // Pestaña 3: Antenas\n        tabbedPane.addTab(\"Antenas\", createAntennasPanel());\n        \n        // Pestaña 4: Filtros y Opciones\n        tabbedPane.addTab(\"Filtros\", createFiltersPanel());\n        \n        add(tabbedPane, BorderLayout.CENTER);\n        \n        // Panel de botones\n        add(createButtonPanel(), BorderLayout.SOUTH);\n        \n        // Configurar ventana\n        setSize(800, 700);\n        setLocationRelativeTo(null);\n    }\n    \n    /**\n     * Panel de configuración de conexión al lector\n     */\n    private JPanel createConnectionPanel() {\n        JPanel panel = new JPanel(new GridBagLayout());\n        panel.setBorder(new EmptyBorder(15, 15, 15, 15));\n        GridBagConstraints gbc = new GridBagConstraints();\n        gbc.fill = GridBagConstraints.HORIZONTAL;\n        gbc.insets = new Insets(5, 5, 5, 5);\n        \n        // IP del lector\n        gbc.gridx = 0; gbc.gridy = 0;\n        panel.add(new JLabel(\"IP del Lector:\"), gbc);\n        \n        gbc.gridx = 1; gbc.weightx = 1.0;\n        txtReaderIP = new JTextField(20);\n        panel.add(txtReaderIP, gbc);\n        \n        // Puerto\n        gbc.gridx = 0; gbc.gridy = 1; gbc.weightx = 0;\n        panel.add(new JLabel(\"Puerto:\"), gbc);\n        \n        gbc.gridx = 1; gbc.weightx = 1.0;\n        txtReaderPort = new JTextField(20);\n        panel.add(txtReaderPort, gbc);\n        \n        // Botón de test de conexión\n        gbc.gridx = 0; gbc.gridy = 2; gbc.gridwidth = 2;\n        btnTestConnection = new JButton(\"Probar Conexión\");\n        btnTestConnection.setIcon(new ImageIcon(\"🔌\"));\n        btnTestConnection.addActionListener(e -> testConnection());\n        panel.add(btnTestConnection, gbc);\n        \n        // Panel de información\n        gbc.gridy = 3;\n        JTextArea infoArea = new JTextArea(\n            \"Configuración de conexión al lector RFID Zebra FX7500\\n\\n\" +\n            \"• IP por defecto: 192.168.1.100\\n\" +\n            \"• Puerto LLRP: 5084\\n\" +\n            \"• Asegúrese de que el lector esté en la misma red\"\n        );\n        infoArea.setEditable(false);\n        infoArea.setBackground(new Color(240, 248, 255));\n        infoArea.setBorder(BorderFactory.createLineBorder(Color.GRAY));\n        panel.add(new JScrollPane(infoArea), gbc);\n        \n        return panel;\n    }\n    \n    /**\n     * Panel de configuración de API\n     */\n    private JPanel createAPIPanel() {\n        JPanel panel = new JPanel(new GridBagLayout());\n        panel.setBorder(new EmptyBorder(15, 15, 15, 15));\n        GridBagConstraints gbc = new GridBagConstraints();\n        gbc.fill = GridBagConstraints.HORIZONTAL;\n        gbc.insets = new Insets(5, 5, 5, 5);\n        \n        // API Endpoint\n        gbc.gridx = 0; gbc.gridy = 0;\n        panel.add(new JLabel(\"API Endpoint:\"), gbc);\n        \n        gbc.gridx = 1; gbc.weightx = 1.0;\n        txtApiEndpoint = new JTextField(30);\n        panel.add(txtApiEndpoint, gbc);\n        \n        // API Key\n        gbc.gridx = 0; gbc.gridy = 1; gbc.weightx = 0;\n        panel.add(new JLabel(\"API Key:\"), gbc);\n        \n        gbc.gridx = 1; gbc.weightx = 1.0;\n        txtApiKey = new JPasswordField(30);\n        panel.add(txtApiKey, gbc);\n        \n        // Mostrar/Ocultar API Key\n        gbc.gridx = 1; gbc.gridy = 2;\n        JCheckBox chkShowApiKey = new JCheckBox(\"Mostrar API Key\");\n        chkShowApiKey.addActionListener(e -> {\n            if (chkShowApiKey.isSelected()) {\n                txtApiKey.setEchoChar((char) 0);\n            } else {\n                txtApiKey.setEchoChar('•');\n            }\n        });\n        panel.add(chkShowApiKey, gbc);\n        \n        // Timeout\n        gbc.gridx = 0; gbc.gridy = 3; gbc.weightx = 0;\n        panel.add(new JLabel(\"Timeout (segundos):\"), gbc);\n        \n        gbc.gridx = 1; gbc.weightx = 1.0;\n        txtApiTimeout = new JTextField(10);\n        panel.add(txtApiTimeout, gbc);\n        \n        // Intervalo de reporte\n        gbc.gridx = 0; gbc.gridy = 4; gbc.weightx = 0;\n        panel.add(new JLabel(\"Intervalo de Reporte (ms):\"), gbc);\n        \n        gbc.gridx = 1; gbc.weightx = 1.0;\n        txtReportInterval = new JTextField(10);\n        panel.add(txtReportInterval, gbc);\n        \n        // Solo tags nuevos\n        gbc.gridx = 0; gbc.gridy = 5; gbc.gridwidth = 2;\n        chkOnlyNewTags = new JCheckBox(\"Enviar solo tags nuevos (no actualizaciones)\");\n        panel.add(chkOnlyNewTags, gbc);\n        \n        return panel;\n    }\n    \n    /**\n     * Panel de configuración de antenas\n     */\n    private JPanel createAntennasPanel() {\n        JPanel mainPanel = new JPanel(new BorderLayout(10, 10));\n        mainPanel.setBorder(new EmptyBorder(15, 15, 15, 15));\n        \n        // Panel de antenas\n        JPanel antennasPanel = new JPanel(new GridLayout(2, 2, 10, 10));\n        \n        for (int i = 1; i <= 4; i++) {\n            AntennaPanel antennaPanel = new AntennaPanel(i);\n            antennaPanels.put(i, antennaPanel);\n            antennasPanel.add(antennaPanel);\n        }\n        \n        mainPanel.add(antennasPanel, BorderLayout.CENTER);\n        \n        // Panel de acciones globales\n        JPanel actionsPanel = new JPanel(new FlowLayout(FlowLayout.LEFT));\n        \n        JButton btnEnableAll = new JButton(\"Habilitar Todas\");\n        btnEnableAll.addActionListener(e -> {\n            antennaPanels.values().forEach(ap -> ap.setEnabled(true));\n        });\n        \n        JButton btnDisableAll = new JButton(\"Deshabilitar Todas\");\n        btnDisableAll.addActionListener(e -> {\n            antennaPanels.values().forEach(ap -> ap.setEnabled(false));\n        });\n        \n        JButton btnResetAll = new JButton(\"Resetear Todas\");\n        btnResetAll.addActionListener(e -> {\n            antennaPanels.values().forEach(AntennaPanel::resetToDefaults);\n        });\n        \n        actionsPanel.add(btnEnableAll);\n        actionsPanel.add(btnDisableAll);\n        actionsPanel.add(btnResetAll);\n        \n        mainPanel.add(actionsPanel, BorderLayout.SOUTH);\n        \n        return mainPanel;\n    }\n    \n    /**\n     * Panel de filtros y opciones avanzadas\n     */\n    private JPanel createFiltersPanel() {\n        JPanel panel = new JPanel(new GridBagLayout());\n        panel.setBorder(new EmptyBorder(15, 15, 15, 15));\n        GridBagConstraints gbc = new GridBagConstraints();\n        gbc.fill = GridBagConstraints.HORIZONTAL;\n        gbc.insets = new Insets(5, 5, 5, 5);\n        \n        // RSSI de corte\n        gbc.gridx = 0; gbc.gridy = 0;\n        panel.add(new JLabel(\"RSSI de Corte (dBm):\"), gbc);\n        \n        gbc.gridx = 1; gbc.weightx = 1.0;\n        txtRssiCutoff = new JTextField(10);\n        panel.add(txtRssiCutoff, gbc);\n        \n        gbc.gridx = 2;\n        JLabel lblRssiInfo = new JLabel(\"(Típico: -70 a -50)\");\n        lblRssiInfo.setFont(lblRssiInfo.getFont().deriveFont(Font.ITALIC, 10f));\n        panel.add(lblRssiInfo, gbc);\n        \n        // Formato de EPC\n        gbc.gridx = 0; gbc.gridy = 1; gbc.gridwidth = 3; gbc.weightx = 0;\n        chkHexFormat = new JCheckBox(\"Mostrar EPC en formato Hexadecimal\");\n        panel.add(chkHexFormat, gbc);\n        \n        // Sesión\n        gbc.gridx = 0; gbc.gridy = 2; gbc.gridwidth = 1;\n        panel.add(new JLabel(\"Sesión:\"), gbc);\n        \n        gbc.gridx = 1; gbc.weightx = 1.0;\n        spnSession = new JSpinner(new SpinnerNumberModel(2, 0, 3, 1));\n        panel.add(spnSession, gbc);\n        \n        gbc.gridx = 2;\n        JLabel lblSessionInfo = new JLabel(\"(0-3)\");\n        lblSessionInfo.setFont(lblSessionInfo.getFont().deriveFont(Font.ITALIC, 10f));\n        panel.add(lblSessionInfo, gbc);\n        \n        // Modo de búsqueda\n        gbc.gridx = 0; gbc.gridy = 3; gbc.weightx = 0;\n        panel.add(new JLabel(\"Modo de Búsqueda:\"), gbc);\n        \n        gbc.gridx = 1; gbc.gridwidth = 2; gbc.weightx = 1.0;\n        String[] searchModes = {\"DualTarget\", \"SingleTarget\", \"SingleTargetReset\"};\n        cmbSearchMode = new JComboBox<>(searchModes);\n        panel.add(cmbSearchMode, gbc);\n        \n        // Panel de información\n        gbc.gridx = 0; gbc.gridy = 4; gbc.gridwidth = 3;\n        JTextArea infoArea = new JTextArea(\n            \"Información sobre filtros:\\n\\n\" +\n            \"• RSSI: Intensidad de señal mínima para considerar un tag\\n\" +\n            \"• Sesión: Controla la persistencia de la bandera de inventario\\n\" +\n            \"• Modo DualTarget: Óptimo para alta densidad de tags\\n\" +\n            \"• Modo SingleTarget: Para pocos tags con lectura rápida\"\n        );\n        infoArea.setEditable(false);\n        infoArea.setRows(6);\n        infoArea.setBackground(new Color(255, 250, 240));\n        infoArea.setBorder(BorderFactory.createLineBorder(Color.GRAY));\n        panel.add(new JScrollPane(infoArea), gbc);\n        \n        return panel;\n    }\n    \n    /**\n     * Panel de botones de acción\n     */\n    private JPanel createButtonPanel() {\n        JPanel panel = new JPanel(new FlowLayout(FlowLayout.RIGHT));\n        panel.setBorder(new EmptyBorder(10, 10, 10, 10));\n        \n        btnSave = new JButton(\"💾 Guardar\");\n        btnSave.setToolTipText(\"Guardar configuración en archivo\");\n        btnSave.addActionListener(e -> saveConfiguration());\n        \n        btnLoad = new JButton(\"📂 Cargar\");\n        btnLoad.setToolTipText(\"Cargar configuración desde archivo\");\n        btnLoad.addActionListener(e -> loadConfiguration());\n        \n        btnExport = new JButton(\"📤 Exportar\");\n        btnExport.setToolTipText(\"Exportar configuración a archivo personalizado\");\n        btnExport.addActionListener(e -> exportConfiguration());\n        \n        btnImport = new JButton(\"📥 Importar\");\n        btnImport.setToolTipText(\"Importar configuración desde archivo\");\n        btnImport.addActionListener(e -> importConfiguration());\n        \n        btnReset = new JButton(\"🔄 Resetear\");\n        btnReset.setToolTipText(\"Restaurar valores por defecto\");\n        btnReset.addActionListener(e -> resetToDefaults());\n        \n        btnApply = new JButton(\"✅ Aplicar y Cerrar\");\n        btnApply.setToolTipText(\"Aplicar configuración y cerrar ventana\");\n        btnApply.setBackground(new Color(76, 175, 80));\n        btnApply.setForeground(Color.WHITE);\n        btnApply.addActionListener(e -> applyAndClose());\n        \n        panel.add(btnSave);\n        panel.add(btnLoad);\n        panel.add(btnExport);\n        panel.add(btnImport);\n        panel.add(btnReset);\n        panel.add(btnApply);\n        \n        return panel;\n    }\n    \n    /**\n     * Carga la configuración actual en los componentes de la UI\n     */\n    private void loadConfigToUI() {\n        txtReaderIP.setText(currentConfig.getReaderHostname());\n        txtReaderPort.setText(String.valueOf(currentConfig.getReaderPort()));\n        txtApiEndpoint.setText(currentConfig.getApiEndpoint());\n        txtApiKey.setText(currentConfig.getApiKey());\n        txtApiTimeout.setText(String.valueOf(currentConfig.getApiTimeout()));\n        txtRssiCutoff.setText(String.valueOf(currentConfig.getRssiCutoff()));\n        txtReportInterval.setText(String.valueOf(current

